module;

#include "engine/application.h"

export module phenyl.application;

export namespace phenyl {
    using Application = phenyl::engine::Application;
    using ApplicationProperties = phenyl::engine::ApplicationProperties;
}
