#pragma once

#include "logging/logging.h"

namespace phenyl::component::detail {
    extern Logger COMPONENT_LOGGER;

    extern Logger COMPONENT_SET_LOGGER;
    extern Logger ENTITY_LOGGER;
    extern Logger MANAGER_LOGGER;
    extern Logger PREFAB_LOGGER;
    extern Logger QUERY_LOGGER;
    extern Logger SERIALIZER_LOGGER;
}